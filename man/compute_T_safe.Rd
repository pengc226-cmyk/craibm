% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{compute_T_safe}
\alias{compute_T_safe}
\title{Compute Safe Fast-Forward Months for Juvenile Recruits}
\usage{
compute_T_safe(
  theta_clean,
  juv_onlyM_len,
  min_adult_age,
  age_recruit = 0,
  g1_a = 1,
  g1_b = 0,
  g1_c = 1.614,
  g1_d_avg = 210.29,
  use_dd_growth = TRUE,
  max_months = 240L
)
}
\arguments{
\item{theta_clean}{DataFrame with columns Linf, K, t0 (VBGF bootstrap).}

\item{juv_onlyM_len}{Fishery entry length threshold (mm).}

\item{min_adult_age}{Age at which fish become adults (density/mortality switch).}

\item{age_recruit}{Age of recruits at entry (usually 0; recruits enter at age 0).}

\item{g1_a, g1_b, g1_c, g1_d_avg}{Juvenile density-dependent growth params (growth_1).}

\item{use_dd_growth}{Logical. If FALSE, growth is not density-dependent
(PG = 1.0 constant); if TRUE, max growth PG = g1_a + g1_b.}

\item{max_months}{Safety cap on iteration (default 240 = 20 years).}
}
\value{
A list with:
  \describe{
    \item{T_safe}{Integer. Safe fast-forward months.}
    \item{T_length}{Months until fastest fish reaches juv_onlyM_len.}
    \item{T_age}{Months until recruit reaches min_adult_age.}
    \item{limiting_factor}{"length" or "age" — which bound is active.}
    \item{fastest_fish_idx}{Which bootstrap row grows fastest.}
  }
}
\description{
Calculates the maximum number of months that newly-recruited
  juveniles can be "fast-forwarded" (batch-simulated via expected survival)
  before any individual could either (a) reach fishery-entry size
  (juv_onlyM_len) or (b) reach adult age (min_adult_age).

  Uses MAXIMUM growth (lowest density, PG = g1_a + g1_b) to be conservative:
  even the fastest-growing bootstrap fish must not exceed juv_onlyM_len
  before T_safe.
}
