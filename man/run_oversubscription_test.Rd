% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{run_oversubscription_test}
\alias{run_oversubscription_test}
\title{Run Parallel Performance and Memory Test}
\usage{
run_oversubscription_test(
  run_one_fn,
  n_cores,
  requested_workers,
  combo_threads,
  omp_threads,
  cluster_setup_fn,
  logical_cores,
  total_tasks = requested_workers,
  mem_abort_frac = 0.95
)
}
\arguments{
\item{run_one_fn}{Function(rep_id) that runs one replicate with the user's
current engine/omp/combo settings. Provided by the app.}

\item{n_cores}{Number of concurrent replicate workers to actually launch
for the benchmark (the probe size, not necessarily the full setting).}

\item{requested_workers}{The full replicate-core setting, for reporting.}

\item{combo_threads}{Policy-combo threads per worker (reporting only).}

\item{omp_threads}{Individual OpenMP threads per model (reporting only).}

\item{cluster_setup_fn}{Function(cl) that library()+clusterExport so workers
can run run_one_fn.}

\item{logical_cores}{Number of detected logical processors.}

\item{total_tasks}{Total number of independent scenario-by-replicate jobs in
the planned full simulation.}

\item{mem_abort_frac}{Fraction of TOTAL physical RAM used as the hard memory
limit. Default is 0.95.}
}
\value{
A named list describing the benchmark result (see $status).
}
\description{
Runs the user's current run-control configuration to estimate
  CPU contention and memory pressure. Memory is classified using three levels:
  safe when projected demand is below currently available RAM; warning when it
  exceeds currently available RAM but remains below the hard total-RAM limit;
  and blocked when it reaches the hard total-RAM limit. The function also
  estimates whether sequential replicate batching can make the plan runnable.
}
